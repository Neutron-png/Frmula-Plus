# FRMULA+ Dashboard

Static dashboard frontend.

## Deploy
- GitHub Pages: upload the `dashboard` folder as a static site.
- Vercel: import the `dashboard` folder as a static project.

## Before deploy
Edit `config.js` and set:
- `API_BASE_URL` to your deployed dashboard API URL
- `ADMIN_KEY` to the same value used by the API
